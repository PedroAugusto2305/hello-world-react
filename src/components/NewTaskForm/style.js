import styled from "styled-components";

const FormStyle = styled.form`
  margin-bottom: 20px;
  display: flex;
  gap: 10px;
`;

const FormTitle = styled.h2`
  font-size: 1.5rem;
  color: #555;
  margin-bottom: 15px;
`;

export { FormStyle, FormTitle };
